# r34poster

 `python -m venv venv`

 `source venv/bin/activate`

### **PRE**

`pip install lxml`

`pip install -r requirements.txt`

`cp backup.env .env`

`python -m bot`

**input.py:**

`tags = [
    {
        "tag": "",
        "channel": "",
        "caption": ""
    },
]`
